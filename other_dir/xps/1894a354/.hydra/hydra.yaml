hydra:
  run:
    dir: /home/thomasyim/cs229-music/other_dir/xps/1894a354
  sweep:
    dir: multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
  launcher:
    _target_: hydra._internal.core_plugins.basic_launcher.BasicLauncher
  sweeper:
    _target_: hydra._internal.core_plugins.basic_sweeper.BasicSweeper
    max_batch_size: null
    params: null
  help:
    app_name: ${hydra.job.name}
    header: '${hydra.help.app_name} is powered by Hydra.

      '
    footer: 'Powered by Hydra (https://hydra.cc)

      Use --hydra-help to view Hydra specific help

      '
    template: '${hydra.help.header}

      == Configuration groups ==

      Compose your configuration from those groups (group=option)


      $APP_CONFIG_GROUPS


      == Config ==

      Override anything in the config (foo.bar=value)


      $CONFIG


      ${hydra.help.footer}

      '
  hydra_help:
    template: 'Hydra (${hydra.runtime.version})

      See https://hydra.cc for more info.


      == Flags ==

      $FLAGS_HELP


      == Configuration groups ==

      Compose your configuration from those groups (For example, append hydra/job_logging=disabled
      to command line)


      $HYDRA_CONFIG_GROUPS


      Use ''--cfg hydra'' to Show the Hydra config.

      '
    hydra_help: ???
  hydra_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][HYDRA] %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
    root:
      level: INFO
      handlers:
      - console
    loggers:
      logging_example:
        level: DEBUG
    disable_existing_loggers: false
  job_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][%(name)s][%(levelname)s] - %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
      file:
        class: logging.FileHandler
        formatter: simple
        filename: ${hydra.runtime.output_dir}/${hydra.job.name}.log
    root:
      level: INFO
      handlers:
      - console
      - file
    disable_existing_loggers: false
  env: {}
  mode: RUN
  searchpath: []
  callbacks: {}
  output_subdir: .hydra
  overrides:
    hydra:
    - hydra.run.dir=/home/thomasyim/cs229-music/other_dir/xps/1894a354
    - hydra.mode=RUN
    task:
    - solver=musicgen/musicgen_base_32khz
    - model/lm/model_scale=small
    - continue_from=//pretrained/facebook/musicgen-small
    - conditioner=text2music
    - dset=audio/finetune
    - dataset.num_workers=2
    - dataset.valid.num_samples=1
    - dataset.batch_size=1
    - schedule.cosine.warmup=8
    - optim.optimizer=adamw
    - optim.lr=1e-4
    - optim.epochs=5
    - optim.updates_per_epoch=1000
    - optim.adam.weight_decay=0.01
    - generate.lm.prompted_samples=False
    - generate.lm.gen_gt_samples=True
    - +output_dir=dora_output
  job:
    name: train
    chdir: null
    override_dirname: +output_dir=dora_output,conditioner=text2music,continue_from=//pretrained/facebook/musicgen-small,dataset.batch_size=1,dataset.num_workers=2,dataset.valid.num_samples=1,dset=audio/finetune,generate.lm.gen_gt_samples=True,generate.lm.prompted_samples=False,model/lm/model_scale=small,optim.adam.weight_decay=0.01,optim.epochs=5,optim.lr=1e-4,optim.optimizer=adamw,optim.updates_per_epoch=1000,schedule.cosine.warmup=8,solver=musicgen/musicgen_base_32khz
    id: ???
    num: ???
    config_name: config
    env_set: {}
    env_copy: []
    config:
      override_dirname:
        kv_sep: '='
        item_sep: ','
        exclude_keys: []
  runtime:
    version: 1.3.2
    version_base: '1.1'
    cwd: /home/thomasyim/cs229-music
    config_sources:
    - path: hydra.conf
      schema: pkg
      provider: hydra
    - path: /home/thomasyim/audiocraft/config
      schema: file
      provider: main
    - path: hydra_plugins.hydra_colorlog.conf
      schema: pkg
      provider: hydra-colorlog
    - path: ''
      schema: structured
      provider: schema
    output_dir: /home/thomasyim/cs229-music/other_dir/xps/1894a354
    choices:
      solver: musicgen/musicgen_base_32khz
      model: lm/musicgen_lm
      model/lm/model_scale: small
      solver/musicgen/evaluation: none
      conditioner: text2music
      dset: audio/finetune
      hydra/env: default
      hydra/callbacks: null
      hydra/job_logging: default
      hydra/hydra_logging: default
      hydra/hydra_help: default
      hydra/help: default
      hydra/sweeper: basic
      hydra/launcher: basic
      hydra/output: default
  verbose: false
